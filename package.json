{
  "name": "walksap",
  "version": "1.0.0",
  "description": "Introdução ao sapUI5",
  "scripts": {
    "start": "ui5 serve -o test/mockServer.html",
    "build": "ui5 build -a",
    "serve-dist": "ws --compress -d dist --open"
  },
  "devDependencies": {
    "@ui5/cli": "^3.7.2",
    "local-web-server": "^5.3.0",
    "ui5-middleware-simpleproxy": "^3.2.8"
  }
}
